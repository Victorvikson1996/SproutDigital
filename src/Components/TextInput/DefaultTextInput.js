import { View, Text } from "react-native";
import React from "react";

const DefaultTextInput = () => {
  return (
    <View>
      <Text>DefaultTextInput</Text>
    </View>
  );
};

export default DefaultTextInput;
